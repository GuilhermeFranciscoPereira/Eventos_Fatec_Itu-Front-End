'use client';
import Modal from '@/components/Modal';
import Toast from '@/components/Toast';

export default function ZustandWrapper(): React.ReactElement {
    return (
        <>
            <Modal />
            <Toast />
        </>
    );
}
